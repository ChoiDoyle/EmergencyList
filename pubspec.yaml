name: emergency_list
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  #responsive screen
  flutter_screenutil: ^5.0.0+2

  #shared preferences (keep user logged in)
  shared_preferences: ^2.0.11

  #firebase
  #firebase_core: ^1.10.0
  firebase_auth: ^3.2.0
  firebase_database: ^8.1.0
  
  fluttertoast: ^8.0.8

  #others
  colorful_safe_area: ^0.3.0

 

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
